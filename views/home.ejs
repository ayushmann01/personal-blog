<%- include('partials/header'); -%>  

<div>
    <h1>Home</h1>

    <% posts.forEach(post => { %>
        <div>
            <h3><%= post.title %></h3>
            <p><%= post.content %></p>
        </div> 
    <% }); %>     
</div>
   
<%- include('partials/footer'); -%>